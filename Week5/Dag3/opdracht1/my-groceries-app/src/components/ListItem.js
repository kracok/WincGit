import React from "react";

function ListItem({item, clickItem}) {
    return (
    <li onClick={} key={item.id} value={item.title}>{item.title}{""}</li>
            //<span item titel 
    )
}

export default ListItem;